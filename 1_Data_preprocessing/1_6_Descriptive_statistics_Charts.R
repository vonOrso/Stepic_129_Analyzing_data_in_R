# Вопрос 1
# library(ggplot2)
# dat <- airquality
# ggplot(dat, aes(x = factor(Month), y = Ozone, group = Month))+geom_boxplot()

# 1.6.1
# plot1 <- ggplot(mtcars, aes(x = mpg, y = disp, col = hp))+geom_point()